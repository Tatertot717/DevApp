name: CI / Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repo
      - uses: actions/checkout@v4

      # 2. Set up Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      # 3. Install dependencies in the devapp folder
      - name: Install dependencies
        working-directory: devapp
        run: npm ci

      - name: Build
        working-directory: devapp
        run: npm run build

      - name: Lint
        working-directory: devapp
        run: npm run lint
